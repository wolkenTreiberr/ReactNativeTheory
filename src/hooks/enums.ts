export enum OrientationType {
  portrait = 'portrait',
  landscape = 'landscape',
}
export enum URLType {
  api = 'https://',
}

export enum Domain {
  unsplash = 'api.unsplash.com/',
}

export enum RequestType {
  fetchPhotos,
  likePhoto,
  unlikePhoto,
  searchPhotos,
}
